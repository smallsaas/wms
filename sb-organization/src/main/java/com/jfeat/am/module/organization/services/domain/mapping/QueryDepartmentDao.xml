<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeat.am.module.organization.services.domain.dao.QueryDepartmentDao">

   <sql id="Base_Column_List">
      t_department.id AS departmentId,
      t_department.sort,
      t_department.pid,
      t_department.name,
      t_department.full_name AS fullName,
      t_department.desc,
      t_department.version,
      t_staff.id AS staffId,
      t_staff.profile_id AS profileId,
      t_staff.work_age AS workAge,
      t_staff.position,
      t_staff.note
    </sql>

<!--    <select id="findDepartmentWithStaff" resultType="map">
        SELECT
        <include refid="DepartmentAndStaff"></include>
        FROM department
        LEFT JOIN staff
        ON staff.dept_id = department.id
        WHERE department.id = #{id}
    </select>-->
</mapper>