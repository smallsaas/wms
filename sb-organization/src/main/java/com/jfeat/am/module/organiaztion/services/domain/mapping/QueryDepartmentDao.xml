<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeat.am.module.organiaztion.services.domain.dao.QueryDepartmentDao">

   <!-- <sql id="DepartmentAndStaff">
        department.id AS departmentId,
        department.sort,
        department.pid,
        department.name,
        department.full_name AS fullName,
        department.desc,
        department.version,
        staff.id AS staffId,
        staff.profile_id AS profileId,
        staff.work_age AS workAge,
        staff.position,
        staff.note
    </sql>

    <select id="findDepartmentWithStaff" resultType="map">
        SELECT
        <include refid="DepartmentAndStaff"></include>
        FROM department
        LEFT JOIN staff
        ON staff.dept_id = department.id
        WHERE department.id = #{id}
    </select>
-->

</mapper>