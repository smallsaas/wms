<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeat.am.module.organization.services.domain.dao.QueryStaffTeamDao">
    <sql id="Base_Column_List">
        t_team_staff.*
    </sql>

    <select id="getTeamLeaders" resultType="Staff" parameterType="Staff">
        SELECT t_staff.* FROM staff
        RIGHT JOIN t_team_staff ON t_team_staff.team_id = #{teamId}
        AND t_team_staff.staff_id=t_staff.id
        AND t_team_staff.is_leader=1
    </select>

    <select id="getTeamsLedByStaff" resultType="Team" parameterType="Team">
        SELECT t_team.* FROM t_team
        RIGHT JOIN t_team_staff ON t_team_staff.team_id = t_team.id
        AND t_team_staff.staff_id= #{staffId}
        AND t_team_staff.is_leader=1
    </select>

</mapper>