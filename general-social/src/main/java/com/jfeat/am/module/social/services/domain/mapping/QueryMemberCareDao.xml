<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeat.am.module.social.services.domain.dao.QueryMemberCareDao">
    <sql id="Base_Column_List">
            so_member_care.*
        </sql>
        <select id="findMemberCarePage" resultType="MemberCare" parameterType="MemberCare">
            SELECT so_member_care.*
            FROM so_member_care
            WHERE 1=1
                             <if test="id != null and id>0 ">
                 AND so_member_care.id LIKE CONCAT('%',#{id},'%')
            </if>
                                             <if test="memberId != null and memberId>0 ">
                 AND so_member_care.member_id LIKE CONCAT('%',#{memberId},'%')
            </if>
                                             <if test="followerId != null and followerId>0 ">
                 AND so_member_care.follower_id LIKE CONCAT('%',#{followerId},'%')
            </if>
                                             <if test="followTime != null and followTime>0 ">
                 AND so_member_care.follow_time LIKE CONCAT('%',#{followTime},'%')
            </if>
                    </select>


    <select id="queryMemberCareWithUserByResId" resultType="map">
        SELECT `user`.avatar,`user`.`name`,`user`.sex,`user`.phone FROM `user`
        LEFT JOIN so_member_care ON `user`.id = so_member_care.follower_id
        WHERE so_member_care.member_id = #{id}
    </select>
</mapper>