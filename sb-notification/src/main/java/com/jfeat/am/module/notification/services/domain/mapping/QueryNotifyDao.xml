<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeat.am.module.notification.services.domain.dao.QueryNotifyDao">

    <select id="queryNotifyByUserIdAndIsReadAndTargetType" resultType="Notify">
        SELECT
        notify.*
        FROM
        notify
        LEFT JOIN user_notify ON notify.id = user_notify.notify_id
        WHERE
        user_notify.user_id = #{userId}
        AND user_notify.is_read = #{isRead}
        AND notify.target_type = #{targetType}
    </select>

    <select id="queryNotify" resultType="Notify">
        SELECT * FROM notify WHERE notify.target_id = #{targetId} AND notify.target_type = #{targetType} AND notify.action = #{action} AND notify.create_time > #{createAt};
    </select>

</mapper>